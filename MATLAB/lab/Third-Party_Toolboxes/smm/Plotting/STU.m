function textOut = STU(textIn)
% calls SaveTheUnderscores
%pass a text string or a cell array of text strings and all "_" will be
%replaced by "\_" and returned
% tag:underscore
% tag:text
% tag:save
textOut = SaveTheUnderscores(textIn);
return