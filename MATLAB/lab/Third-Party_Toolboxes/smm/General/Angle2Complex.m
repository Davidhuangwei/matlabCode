% function complexNum = Angle2Complex(angle)
% complexNum = complex(cos(angle),sin(angle));
% tag:angle
% tag:complex
% tag:cos
% tag:sin
function complexNum = Angle2Complex(angle)
complexNum = complex(cos(angle),sin(angle));
return