function setftype(fn,ftype,fcrea)% SETFTYPE  Change file type and creator of a Macintosh file.%%   Superceded by FILETYPE.  Included only for backward compatibility.%%   SETFTYPE(FN,FTYPE,FCREA) changes the Macintosh file type and creator%   of file FN to FTYPE and FCREA, respectively.  If FCREA is omitted,%   it is set to 'MATL' (Matlab); if FCREA is supplied, but an empty%   string, then the creator attribute is left unchanged.%%   If FN is a string matrix, each row (minus trailing spaces and NULL%   characters) is taken to be a filename, each of which is changed to%   the given file type and creator.%%   The number of rows in FTYPE and FCREA must be one or the same as the%   number of rows in FN.%%  See also FILETYPE.% Copyright (c)1995-97, Erik A. Johnson <johnsone@uiuc.edu>, 3/23/97% warning about obsolete routinemsg = 'SETFTYPE has been replaced by FILETYPE.';v=version;if (v(1)<'5'),	disp(['Warning: ' msg]);else,	feval('warning',msg);end;% check the argumentsif (nargin<2),  return; end;if (nargin>3),  error('FILETYPE takes at most 3 input arguments'); end;if (nargout>0), error('SETFTYPE returns no outputs.'); end;% call the newer version for the workif (nargin<3), filetype(fn,ftype,'MATL');else,          filetype(fn,ftype,fcrea);end;